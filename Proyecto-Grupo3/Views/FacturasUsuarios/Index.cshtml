@model IEnumerable<Proyecto_Grupo3.Models.TFacturasUsuario>

@{
    ViewData["Title"] = "Facturas por Usuario";
}

<main class="my-5">
    <div class="containern p-3">
        <div class="row pt-4">
            <div class="col-6">
                <h2 class="text-primary">Facturas por Usuario</h2>
            </div>
        </div>
        <br /><br />

        <table class="table table-striped table-hover" style="width:100%">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.IdCliente)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.IdFactura)
                    </th>

                    </th>
                </tr>
            </thead>
            <tbody>
        @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.IdClienteNavigation.IdCliente)
                    </td>
                    <td>
                        @Html.DisplayNameFor(model => model.IdFacturaNavigation)
                    </td>
                    <td>
                        <div class=" w-75 btn-group" role="group">
                            <a asp-controller="FacturasUsuarios" asp-action="Details" asp-route-id="@item.IdFacturasUsuario" class="btn btn-outline-info mx-2"> <i class="bi bi-info-circle"></i>Consultar</a>
                            <a asp-controller="FacturasUsuarios" asp-action="Edit" asp-route-id="@item.IdFacturasUsuario" class="btn btn-outline-primary mx-2"> <i class="bi bi-pencil-square"></i>Editar</a>
                            <a asp-controller="FacturasUsuarios" asp-action="Delete" asp-route-id="@item.IdFacturasUsuario" class="btn btn-outline-danger mx-2"> <i class="bi bi-trash-fill"></i>Eliminar</a>
                        </div>
                    </td>
                </tr>
        }
            </tbody>
        </table>
    </div>
</main>